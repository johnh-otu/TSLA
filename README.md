# TSLA Stock Prediction Model

## To train the model:
In command prompt, in the repos directory:
```cmd
python train.py
```
## To predict with model:
In command prompt, in the repos directory:
```cmd
python predict.py
```
